// name, id, email properties
// getName(), getId(), getEmail(), getRole() methods

class Employee {
    constructor(name, id, email) {
        this.name = name;
        this.id = id;
        this.email = email;
    }
}